import React from "react";
import {TextFields} from "@material-ui/icons";
import Box from "@material-ui/core/Box";

export const Homepage = (props) => {
  return <Box>
    <TextFields>Homepage</TextFields>
  </Box>
}